version: 0.2

phases:
  install:
    runtime-versions:
      docker: 18
      python: 3.7
  build:
    commands:
      - pwd
      - python --version
      - docker version
    finally:
      - echo finally